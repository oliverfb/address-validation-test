services:
  api:
    build:
      context: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      # Allow override from .env / deploy environment
      PORT: "3000"
    restart: unless-stopped

